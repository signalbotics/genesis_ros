#!/bin/sh
set -e
case "$1" in
    remove|upgrade|deconfigure)
        # Nuke __pycache__ so dpkg doesn't complain about leftover .pyc files.
        find /opt/genesis/venv -name __pycache__ -type d -exec rm -rf {} + 2>/dev/null || true
        ;;
esac
#DEBHELPER#
exit 0
